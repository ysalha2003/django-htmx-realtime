# Generate a strong secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=x+7knbem0+@o*a42=mn84pd+^$zdv604d^o5$9eyt(08+oe^pu

# Set to False in production
DEBUG=True

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,95.179.252.66

# Redis URL for channels
REDIS_URL=redis://localhost:6379/0

# Email backend (console for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Security settings (set to True in production with HTTPS)
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
